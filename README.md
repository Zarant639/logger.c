# logger.c
Age Logger — a simple console tool written in C.  Features: - Input validation (only numbers, age range 0–127) - Memory allocation with error handling (malloc/perror) - Age restrictions (e.g., “Baby” for &lt;14) - Time logging with timestamps - Configurable max attempts (default 3)  Logs are saved to age.log in the current directory.
